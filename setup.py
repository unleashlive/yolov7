import setuptools

setuptools.setup(
    name="yolov7_segmentation",
    version="1.0.1",
    packages=setuptools.find_packages(),
    include_package_data=True,
    install_requires=[],
    extras_require={},
    setup_requires=[],
    tests_require=[],
)